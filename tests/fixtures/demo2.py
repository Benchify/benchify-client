"""
Another empty file just for testing resolve_local_imports_recursive
(Well, it is not empty, but it's empty of stuff that actually matters ...)
"""
from .demo3 import banana, orange

def blarg(lst):
    return ((lst, lst), orange(banana))